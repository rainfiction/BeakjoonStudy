1.
작년에 배울 때는 별 생각이 없었는데... C랑 C++배우고 나서 보니 이렇게 놀라운 언어가 있을 수가 없음. 컴퓨터가 알잘딱깔센하는 범위가 놀라울 정도로 넓다. 너무...너무 신기함. 세상에, 어떻게 이렇게 편하게 쓸 수가 있지...? 


2.
기호의 차이점.
// 는 몫을, %는 나머지를, /는 그냥 소수 단위로 값을 계산해버린다.
제곱 계산에 함수를 쓸 필요 없이 **을 사용하여 바로바로 계산이 가능하다.


3.
a++, a--, ++a, --a...같은 증감 연산자를 사용할 수 없다.


4.
파이썬에서는 ""와 ''의 기능적인 차이가 전혀 없다. 와우.


5.
== : 값이 동일한지 확인함
is : 참조가 동일한지 확인함. 즉 같은 주소값을 가지는 동일한 변수일 경우에만 True반환


6. 
del : 변수를 메모리에서 삭제하기 위해 사용하는 명령어


7.
input() 으로 입력받는 값은 기본적으로 문자형(str)이다. 원하는 대로 쓰려면 직접 형변환 해줘야 함.


8.
큰 수 처리에 큰 장점이 있는 언어. 천자리가 넘는 큰 수도 특별한 조치 없이 그냥 한 방에 저장된다ㄷㄷ
큰 수 관리가 필요한 문제는 그냥 파이썬으로 풀어버리는 게 제일 간단할듯


9.
주석이  //나 /**/ 이 아닌 #이다.


10.
else문은 기존의 기능에 더해 어떤 조건이 완전히 끝났을 때 한 번 더 실행해 주는 역할도 한다.